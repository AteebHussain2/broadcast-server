{
  "name": "broadcast-server",
  "version": "1.0.0",
  "description": "This project implements a websocket server that assigns temporary usernames to clients upon connection. The server handles client connections, broadcasts messages, and ensures each client has a unique temporary username.",
  "license": "MIT",
  "author": "AteebHussain2",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "readline": "^1.3.0",
    "ws": "^8.19.0"
  }
}
